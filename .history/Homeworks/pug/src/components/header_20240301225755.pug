mixin header
  header.header
    .wrapper.header-wrapper
      +header-logo
      +navigation 
      .header-buttons
        +button('Sign In', 'button', 'red')
        +button('Sign Up', 'button', 'classes')









